/**
 * Initialize `create recording`
 */
require(['/assets/javascript/requireconfig.js'], function () {
    require([
        'dropdownToggle', 
        'brand/modules/checkmate-init', 
        'brand/modules/recording-editors'
    ]);
});
