/**
 * Initialize `brand`
 */
require(['requireconfig'], function () {
    require([
        'dropdownToggle',
        'brand/modules/checkmate-init',
        'brand/modules/batch-handler'
    ]);
});
