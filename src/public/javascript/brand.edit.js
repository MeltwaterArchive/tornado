/**
 * Initialize `brand`
 */
require(['requireconfig'], function () {
    require([
        'dropdownToggle',
        '/assets/javascript/requireconfig.js',
        'brand/modules/delete-handler'
    ]);
});
